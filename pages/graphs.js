import NavWrapper from '../components/NavWrapper';
import GraphsPage from '../components/GraphPage';

const Graphs = () => {
  return (
    <NavWrapper>
      <GraphsPage />
    </NavWrapper>
  );
};

export default Graphs;
